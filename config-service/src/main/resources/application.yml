---
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/willianmews/microservice-spring/
          searchPaths: ConfigData
        native:
          searchLocations: classpath:offline-repository/
server:
  port: 8001